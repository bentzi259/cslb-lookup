apiVersion: v1
kind: Service
metadata:
  name: {{ include "cslb-license-api.fullname" . }}
  labels:
    {{- include "cslb-license-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cslb-license-api.selectorLabels" . | nindent 4 }}
